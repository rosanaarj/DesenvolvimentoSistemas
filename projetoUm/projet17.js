//  Crie um array de produtos e use forEach() para exibir cada elemento no console.
let produtos = ["Creme", "Perfume", "Hidratante", "Gloss", "Jóias"];

produtos.forEach(function(produto) {
    console.log(produto);
});
