/** 1) Crie um array chamado cores com três cores diferentes
 * Adicione uma nova cor ao final do array.
 * Remova a primeira cor do array.
 * Mostre o array final no console.
 */

let cores = ['rosa', 'roxo', 'azul'];
cores.push('lilás');
cores.shift();
console.log(cores);
